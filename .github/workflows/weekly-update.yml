name: Weekly Job Data Update

on:
  schedule:
    # 台灣時間週一早上 08:00 (UTC 00:00)
    - cron: '0 0 * * 1'
  workflow_dispatch: # 允許手動觸發

permissions:
  contents: write

jobs:
  update-data:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        
    - name: Run scraper and update data
      env:
        # 如果需要，可以在 GitHub Secrets 設定這些變數
        SEARCH_KEYWORDS: "行銷,產品經理"
        SCRAPY_PAGES_PER_KEYWORD: "5"
      run: |
        python update_dashboard.py
        
    - name: Commit and push changes
      run: |
        git config --global user.name 'github-actions[bot]'
        git config --global user.email 'github-actions[bot]@users.noreply.github.com'
        git add data.csv
        # 只有在有變更時才 commit
        git diff --quiet && git diff --staged --quiet || (git commit -m "🤖 Auto-update job data $(date +'%Y-%m-%d')" && git push)
