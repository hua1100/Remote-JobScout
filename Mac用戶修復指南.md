# Mac用戶快速修復指南 🍎

## 問題說明
Mac使用Homebrew的Python會有「externally-managed-environment」錯誤,這是Python 3.11+的新安全機制。

## 解決方案 (3選1)

---

### ✅ 方案1: 使用虛擬環境 (推薦)

這是最安全的做法:

```bash
# 1. 建立虛擬環境
python3 -m venv venv

# 2. 啟動虛擬環境
source venv/bin/activate

# 3. 安裝套件
pip install -r requirements.txt

# 4. 執行爬蟲
scrapy crawl 104_ai_jobs

# 5. 使用完畢後可關閉虛擬環境
deactivate
```

**或直接執行我提供的腳本:**
```bash
chmod +x run_scraper_venv.sh
./run_scraper_venv.sh
```

---

### ⚡ 方案2: 使用 pipx (簡單)

```bash
# 1. 安裝 pipx
brew install pipx
pipx ensurepath

# 2. 重新開啟終端機,然後安裝 scrapy
pipx install scrapy

# 3. 安裝其他套件
pip3 install --user python-dotenv

# 4. 執行爬蟲
scrapy crawl 104_ai_jobs
```

---

### ⚠️ 方案3: 使用 --break-system-packages (不推薦)

這會繞過保護機制,可能影響系統穩定性:

```bash
# 安裝套件
pip3 install --break-system-packages -r requirements.txt

# 執行爬蟲
scrapy crawl 104_ai_jobs
```

---

## 我應該選哪個方案?

| 方案 | 優點 | 缺點 | 推薦度 |
|------|------|------|--------|
| **虛擬環境** | 最安全、不影響系統 | 需要每次啟動 | ⭐⭐⭐⭐⭐ |
| **pipx** | 簡單快速 | 套件隔離性稍弱 | ⭐⭐⭐⭐ |
| **break-system-packages** | 最快 | 可能破壞系統 | ⭐⭐ |

**建議使用方案1(虛擬環境)**

---

## 快速檢查清單

完成安裝後,測試是否成功:

```bash
# 如果用虛擬環境,先啟動
source venv/bin/activate

# 測試 scrapy 是否可用
scrapy version

# 測試 python-dotenv 是否可用
python3 -c "import dotenv; print('✓ dotenv已安裝')"

# 如果都沒問題,執行爬蟲
scrapy crawl 104_ai_jobs
```

---

## 常見問題

### Q: 為什麼會出現這個錯誤?
A: Python 3.11+ 為了保護系統環境,不允許直接用 pip 安裝套件到系統。

### Q: 虛擬環境是什麼?
A: 一個獨立的Python環境,不會影響系統的Python。就像為這個專案建立一個專屬空間。

### Q: 每次都要啟動虛擬環境嗎?
A: 是的,每次開啟新的終端機視窗時,都需要執行 `source venv/bin/activate`

### Q: 如何知道虛擬環境是否啟動?
A: 終端機提示符前面會出現 `(venv)` 字樣

```bash
# 未啟動
username@macbook project %

# 已啟動
(venv) username@macbook project %
```

### Q: 可以刪除虛擬環境嗎?
A: 可以,直接刪除 `venv` 資料夾即可。需要時再重新建立。

---

## 完整步驟(含截圖說明)

### Step 1: 開啟終端機
按 `Command + Space`,輸入 `Terminal`,按Enter

### Step 2: 切換到專案目錄
```bash
cd ~/Downloads/104-scraper-package
# (根據你的解壓縮位置調整路徑)
```

### Step 3: 建立虛擬環境
```bash
python3 -m venv venv
```
會建立一個名為 `venv` 的資料夾

### Step 4: 啟動虛擬環境
```bash
source venv/bin/activate
```
看到提示符變成 `(venv) ...` 就成功了

### Step 5: 安裝套件
```bash
pip install -r requirements.txt
```
等待安裝完成(約1-2分鐘)

### Step 6: 執行爬蟲
```bash
scrapy crawl 104_ai_jobs
```
等待5-10分鐘完成

### Step 7: 查看結果
```bash
ls -lh ai_jobs_*.csv
open ai_jobs_*.csv
```

---

## 未來使用

下次要執行爬蟲時:

```bash
# 1. 切換到專案目錄
cd ~/Downloads/104-scraper-package

# 2. 啟動虛擬環境
source venv/bin/activate

# 3. 執行爬蟲
scrapy crawl 104_ai_jobs

# 4. 完成後關閉虛擬環境(可選)
deactivate
```

---

## 一鍵執行腳本

我已經為你準備好一鍵執行腳本:

```bash
chmod +x run_scraper_venv.sh
./run_scraper_venv.sh
```

這個腳本會:
1. ✅ 自動建立虛擬環境
2. ✅ 自動安裝套件
3. ✅ 自動執行爬蟲
4. ✅ 完成後保持虛擬環境啟動

---

需要更多協助請參考「完整安裝指南.md」!
